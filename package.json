{
  "name": "node-semaphoreci",
  "version": "1.0.2",
  "description": "A Javascript wrapper for the Semaphore CI API",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel --presets es2015 -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha --compilers js:babel-core/register"
  },
  "keywords": [
    "semaphoreci"
  ],
  "author": "ben-z",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0",
    "mocha": "^2.5.3"
  },
  "engines": {
    "node": ">=4"
  },
  "dependencies": {
    "axios": "^0.12.0"
  },
  "directories": {
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ben-z/node-semaphoreci.git"
  },
  "bugs": {
    "url": "https://github.com/ben-z/node-semaphoreci/issues"
  },
  "homepage": "https://github.com/ben-z/node-semaphoreci#readme"
}
